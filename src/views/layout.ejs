<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "FindMe Argent" %></title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css" rel="stylesheet" />

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <!-- Floating Background Hearts -->
  <div class="hearts-layer">
    <i class="bi bi-heart-fill heart" style="left:10%;animation-delay:0s;"></i>
    <i class="bi bi-heart-fill heart" style="left:25%;animation-delay:4s;"></i>
    <i class="bi bi-heart-fill heart" style="left:50%;animation-delay:8s;"></i>
    <i class="bi bi-heart-fill heart" style="left:75%;animation-delay:12s;"></i>
    <i class="bi bi-heart-fill heart" style="left:90%;animation-delay:16s;"></i>
  </div>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg fixed-top">
    <div class="container-fluid px-4">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <i class="bi bi-heart-fill me-2 text-danger"></i> FindMe Argent
      </a>

      <div class="d-flex align-items-center ms-auto">
        <% if (user) { %>
          <span class="welcome-text">Welcome, <%= user.firstname %></span>
          <a href="/logout" class="btn btn-outline-danger btn-sm">
            <i class="bi bi-box-arrow-right me-1"></i> Logout
          </a>
        <% } else { %>
          <a href="/auth/login" class="btn btn-light border me-2">
            <i class="bi bi-person"></i> Login
          </a>
        <% } %>
      </div>
    </div>
  </nav>

  <!-- Dynamic Content -->
  <main class="container">
    <%- typeof body !== 'undefined' ? body : '' %>
    <%- typeof content !== 'undefined' ? content : '' %>
  </main>

  <!-- Footer -->
  <footer>
    <small>Copyright © <%= new Date().getFullYear() %> <strong>FindMe Argent</strong>. Made with 💜</small>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
